[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py39"
line-length = 120
exclude = [
  ".git",
  ".pytest_cache",
  ".ruff_cache",
  ".cache",
  "backend/models/models",
  "logs",
]

[tool.ruff.lint]
select = ["E", "F", "W"]
